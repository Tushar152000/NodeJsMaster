const add=(a, b)=>{ // perameter has been passed in this case 
     return a+b;
}
const sub=(a, b)=>{ // perameter has been passed in this case 
    return a-b;
}
const name="vinod"; 
module.exports=add;

// In individual file every file is called Module 
//node package manager -which is free to use and it is javascript package
